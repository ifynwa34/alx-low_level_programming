#include <stdio.h>
/**
 * main -  Entry point to printing alphabet with putchar
 * Return: 0
 */
int main(void)
{
	char w;

	w = 'a';

	while
		(w <= 'z')
		{ putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}
