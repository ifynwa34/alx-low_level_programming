#include <stdio.h>
/**
 * main - Entry point to print single digit numbers of base ten
 *
 * Return: 0
 */
int main(void)
{
int f;
for (f = 0 ; f < 10 ; f++)
printf("%d", f);
printf("\n");
return (0);
}
